'use strict';

/**
 * @module heremaps.map
 * @name HeremapsMap
 * @description Map module - wrapper for H.map
 */

var
  angular = require('angular'),
  map = angular
    .module('heremapsMap', []);

module.exports = map;
